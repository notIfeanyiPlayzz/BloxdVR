# Contributing to BloxdVR 🎮
We love help! To contribute to the **v1.3.6** engine:
1. **Fork** the repo and create your feature branch.
2. Ensure your code works in the **Meta Quest Browser**.
3. Keep **JavaScript** lean (under 50ms per frame).
4. Submit a PR with the `(version)-(type)` format.
